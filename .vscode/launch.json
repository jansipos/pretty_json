{
  "version": "0.2.0",
  "configurations": [
      {
        "name": "LLDB - Launch",
        "type": "lldb",
        "request": "launch",
        "program": "${workspaceFolder}/main",
        "args": ["test-data/simple.json"],
        "cwd": "${workspaceFolder}",
        "preLaunchTask": "C/C++: clang++ build active file",
      }
  ]
}
